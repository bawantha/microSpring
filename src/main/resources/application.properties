spring.application.name=microSpring

# MongoDB (Docker on localhost)
spring.data.mongodb.uri=mongodb://localhost:27017/microspring

# Disable OAuth2 auto-config until credentials are set
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.client.reactive.ReactiveOAuth2ClientAutoConfiguration
